import 'package:flutter/material.dart';

class GroupNotice extends StatelessWidget {
  const GroupNotice({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}

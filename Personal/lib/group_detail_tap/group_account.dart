import 'package:flutter/material.dart';

class GroupAccount extends StatelessWidget {
  const GroupAccount({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
